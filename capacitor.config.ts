import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beatrizlcdev.mastermindappmobile',
  appName: 'MastermindAppMobile',
  webDir: 'dist'
};

export default config;
